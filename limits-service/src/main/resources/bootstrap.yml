server:
  port: 8080
spring:
  application:
    name: limits-service
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: dev
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
